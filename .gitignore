.env
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
.vscode/
.idea/
*.swp
dist/
build/
.coverage
htmlcov/
# Batch test reports are regenerated locally; keep only goldens.
reports/

# Personal image assets for POC scripts (too heavy for git; ~20MB each).
# Add your own JPG/PNG files to images/ for local vision experiments.
# The images used in scripts/vision_poc.py are not required for production.
images/

# Terraform
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.backup
**/terraform.tfstate.*.backup
**/tfplan
**/terraform.tfvars
# Keep examples, never commit real tfvars
!**/terraform.tfvars.example
*.tfplan*

# Local agent/editor state
.agents/
.claude/
